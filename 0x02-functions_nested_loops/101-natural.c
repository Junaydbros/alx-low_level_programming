#include <stdio.h>

/**
 * main - a program that computes and prints the sum of all multiples of 3 or 5
 * These multiples below 1024 should be printed
 *
 * Return: void
 */

int main(void)
{
	int n;

	if (n < 1024)
	{
		if (n % 3 == 0)
		{
